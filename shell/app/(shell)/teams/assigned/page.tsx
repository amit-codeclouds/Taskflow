import AssignedTeamsScreen from '@/components/teams/AssignedTeamsScreen';

export const metadata = { title: 'Assigned Teams — Taskflow' };

export default function AssignedTeamsPage() {
  return <AssignedTeamsScreen />;
}
